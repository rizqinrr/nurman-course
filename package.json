{
  "name": "nurman-course-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@12.0.1",
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\""
  },
  "devDependencies": {
    "turbo": "^2.0.4"
  }
}
